# 装备扫描静态映射

- `hero-names.json`：游戏英雄代码到 Fribbels 英文名称的兼容映射。
- `equipment-levels.json`：装备代码到物品等级的兼容映射。
- `equipment-types.json`：部分不能仅凭代码末尾可靠判断的装备部位映射。

这些文件只包含公开的游戏代码、名称、等级和部位，不包含账号、角色实例、装备实例或抓包数据。出现新英雄或新装备代码时应随版本更新；未知活动装备仍会导出，并按不可重铸的 88 级装备及其固定主属性档位修复。
